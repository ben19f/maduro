создать виртуальное окружение
усстановить зависимости
в файле обновителя БД проверить и создать саму базу в нужном месте

создал таблицы в БД все 4

проверяем что таблицы заполнились

запускаем приложение арр.ру


сделал из браузера запрос
fetch(http://127.0.0.1:25400, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: data
    })
    .then(response => response.json())
    .then(data => {
        const container = document.getElementById('public-posts-container');
        if (isInitialLoad) {
            container.innerHTML = '';
        }
     });

     const data = JSON.stringify({
        branch_id: 'main_branch',
        last_post_id: 1,
        date_from: startDate,
        date_to: endDate,
        amount_from: minAmount,
        amount_to: maxAmount,
        post_sender: senderData,
        min_post_id: minId,
        max_post_id: maxId,
        post_hash: hash|| null,
        site_version: '0'
    });
    fetch(url, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: data
    })